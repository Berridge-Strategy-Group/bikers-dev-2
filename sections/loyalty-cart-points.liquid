{% schema %}
{
  "name": "Loyalty — Cart points",
  "settings": [
    { "type": "range", "id": "earn_rate", "label": "Points per NZD", "default": 1, "min": 0, "max": 10, "step": 0.1 }
  ],
  "enabled_on": { "templates": ["cart"] },
  "presets": [{ "name": "Loyalty — Cart points" }]
}
{% endschema %}
